---
/**
 * DEPRECATED — no longer in use.
 *
 * Pages are now written as flat markdown inside .mdx files; subsections are
 * created with regular `##` / `###` headings rather than a wrapper component.
 *
 * This file is kept only because the mounted filesystem does not permit
 * deletion. It is not imported anywhere and can safely be removed in any
 * environment where `rm` succeeds.
 */
---
